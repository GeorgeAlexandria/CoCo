<ResourceDictionary
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
   xmlns:local="clr-namespace:CoCo.UI"
   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   mc:Ignorable="d">
   <Style x:Key="DeleteButton" TargetType="Button">
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Opacity" Value="0.5" />
      <Setter Property="Template">
         <Setter.Value>
            <ControlTemplate>
               <Border
                  Background="Transparent"
                  BorderThickness="0"
                  Focusable="False">
                  <Path
                     Width="12"
                     Height="12"
                     VerticalAlignment="Center"
                     Data="M0,2 L2.5,2 L4,1 L6,1 L7.5,2 L10,2 L10,3 L0,3z M1,4 L9,4 L9,10 Q9,12 7,12 L3,12 Q1,12 1,10z"
                     Fill="{TemplateBinding Foreground}"
                     Stretch="Fill"
                     ToolTip="Delete" />
               </Border>
               <ControlTemplate.Triggers>
                  <Trigger Property="IsMouseOver" Value="True">
                     <Setter Property="Opacity" Value="1" />
                  </Trigger>
               </ControlTemplate.Triggers>
            </ControlTemplate>
         </Setter.Value>
      </Setter>
   </Style>
   <Style x:Key="ApplyPresetButton" TargetType="Button">
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Opacity" Value="0.5" />
      <Setter Property="Template">
         <Setter.Value>
            <ControlTemplate>
               <Border
                  Background="Transparent"
                  BorderThickness="0"
                  Focusable="False">
                  <Path
                     Width="12"
                     Height="12"
                     VerticalAlignment="Center"
                     Data="M3.5,0 L8.5,0 L8.5,3 L12,3 L6,7 L0,3 L3.5,3 z M0,8 L12,8 L12,9 L0,9z"
                     Fill="{TemplateBinding Foreground}"
                     Stretch="Fill"
                     Stroke="Transparent"
                     ToolTip="Apply" />
               </Border>
               <ControlTemplate.Triggers>
                  <Trigger Property="IsMouseOver" Value="True">
                     <Setter Property="Opacity" Value="1" />
                  </Trigger>
               </ControlTemplate.Triggers>
            </ControlTemplate>
         </Setter.Value>
      </Setter>
   </Style>
</ResourceDictionary>